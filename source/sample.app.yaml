# ------------------------------------------------------------------------------
# configuration: copy this file to ``app.yaml`` and set these to suit your app!
# ------------------------------------------------------------------------------

application: new-project-template
version: 1
runtime: python
api_version: 1

handlers:
- url: /static
  static_dir: static

- url: .*
  script: main.py
